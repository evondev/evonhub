export enum Role {
  ADMIN = "ADMIN",
  USER = "USER",
}
export enum TCourseInfoType {
  REQUIREMENTS = "requirements",
  QA = "qa",
  GAINED = "gained",
}
export enum ECourseStatus {
  PENDING = "pending",
  APPROVED = "approved",
  REJECTED = "rejected",
}
